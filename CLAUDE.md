@AGENTS.md

# CLAUDE.md — smartbox-admin

Contexto operativo para trabajar en este repo. El **documento oficial de producto** (Functional Spec, Architecture Guide, API Contract, Product Backlog, Roadmap, Definition of Done, Criterios de aceptación) es la fuente de verdad si hay conflicto con este archivo:

**https://claude.ai/code/artifact/9944c8ca-31a6-4695-b6b2-c11b8e58aeb7**

## Qué es esto

Panel de administración (Next.js 16 App Router + React 19 + Tailwind 4) de **SmartBox**, un SaaS de gestión para gimnasios. Consume el backend en [`smartbox-backend`](../smartbox-backend) vía `NEXT_PUBLIC_API_URL`. El control físico de cápsulas/lockers vía IoT es la última release del roadmap ("SmartBox IoT"), no la primera.

## Dónde estamos

- **Backend: SmartBox v1.0 completo** (Epics 0-5 — identidad, multi-tenant, membresías/Mercado Pago, reservas, operación del gimnasio, observabilidad), en progreso sobre v1.5 (Epic 6) y con Fase 1 del roadmap post-v1.5 (Check-in, Dashboard financiero, Lista de espera) implementada 2026-07-24. Este frontend cubre Auth/Users (Epic 0/1), Gimnasios (Epic 1), Plans/Memberships (Epic 2, incluyendo `E6-04`), Classes/Reservations (Epic 3), Shifts/Reports (Epic 4), el signup self-serve de `E6-05`, el **portal de socios** de `E6-02` (`/portal`) y los 3 ítems de Fase 1 (ver más abajo) — alineado con todo v1.0 más lo que ya avanzó de Epic 6 y Fase 1. Antes de asumir que algo "no está implementado en el backend", confirmalo contra Swagger (`/docs`) — puede que ya exista y solo falte la UI.
- **Funcional hoy**: login/registro/recuperación de contraseña, **`/signup-gym`** (`E6-05`, 2026-07-20: página pública — sin login — donde un dueño de gimnasio prospecto crea su gimnasio y su cuenta ADMIN en un solo paso, con auto-login y redirect a Configuración; el login tiene un link nuevo hacia acá), gestión de usuarios (CRUD completo), perfil editable (`/dashboard/profile` y `/portal/profile`, mismo `ProfileContent` compartido), `/unauthorized`, **Gimnasios** (`/dashboard/gyms`, SUPER_ADMIN: alta/listado/detalle — alta manual, para cuando SUPER_ADMIN necesita crear un gym sin pasar por el signup self-serve) y **Configuración** (`/dashboard/settings`, ADMIN/STAFF: datos del propio gimnasio) con el flujo completo de conexión a Mercado Pago (modelo Marketplace — el gimnasio pega su propio access token, ya no es OAuth, ver `2026-07-23` más abajo y `CLAUDE.md` del backend). Sobre esa misma base, **Planes de membresía** (`E6-04`, 2026-07-20: un gimnasio puede tener varios niveles/tiers — alta desde el detalle del gimnasio para SUPER_ADMIN o desde Configuración para ADMIN, STAFF/CLIENT solo lectura), **Clases** (`/dashboard/classes`, catálogo de turnos recurrentes semanales — SUPER_ADMIN/ADMIN dan de alta, STAFF solo lectura; sin CLIENT desde `E6-02`, ver más abajo) y **Reservas** (`/dashboard/reservations`, ADMIN/SUPER_ADMIN ven y cancelan las del gimnasio; STAFF solo lectura, sin CLIENT desde `E6-02`), **Turnos de Staff** (`/dashboard/shifts`, horarios de trabajo recurrentes — SUPER_ADMIN/ADMIN dan de alta, STAFF solo lectura; sin CLIENT, es operativo) y **Reportes** (`/dashboard/reports`, ocupación de clases con rango de fechas para ADMIN/STAFF/SUPER_ADMIN, ingresos + socios activos solo para ADMIN/SUPER_ADMIN — `GET /reports/revenue` es ADMIN-only en el backend). El resto del dashboard (Pods IoT, Pagos, Monitoreo IoT) sigue siendo "Próximamente" explícito, no datos inventados.
- **`E6-05`: signup público completo; del panel multi-tenant, solo "socios activos por gimnasio"** — el backlog oficial junta "onboarding self-serve" y "panel multi-tenant para SUPER_ADMIN" bajo la misma historia. Primera sesión de scoping (2026-07-20): se implementó el signup público. Segunda sesión de scoping, mismo día, para la otra mitad: de cuatro métricas ofrecidas (socios activos por gym, ingresos recientes por gym, conteo de planes/clases, cards de resumen global), el usuario eligió **solo socios activos por gimnasio** — `GymTable` (`/dashboard/gyms`) ahora tiene una columna "Socios activos" (`Gym.activeMembersCount`, viene calculado del backend en `GET /gyms`). Las otras tres métricas ofrecidas NO están implementadas — no asumirlas si aparece una historia futura sobre este panel.
- **`E6-01`/`E6-02`: portal de socios (`/portal`), implementado 2026-07-20** — `E6-01` era una decisión de producto sin código ("no se escribe código de portal hasta decidir web vs. app nativa"): se decidió **portal web ligero**, extendiendo este mismo repo, no una app nativa. Eso desbloqueó `E6-02` ("autogestión ampliada: reservas, historial de pagos, método de pago, desde fuera del panel admin"). Antes de programar se verificó que reservas e historial de pagos ya existían para CLIENT (Epic 3, `E2-07`) y que "método de pago" ya estaba descartado en `E2-07` (sin UI de tarjeta, el socio usa su propia cuenta de Mercado Pago) — lo único genuinamente nuevo era sacar esas vistas del shell operativo. Sesión de scoping, 2026-07-20: se construyó una superficie separada `/portal` (nav propio en `PortalNav`, sin sidebar de `/dashboard`, sin branding "SUPER_ADMIN Panel"), no solo se reetiquetó `/dashboard` para CLIENT.
  - `/portal` (Mi Membresía — mismo contenido que el viejo `/dashboard/membership`, que se **borró**), `/portal/classes` + `/portal/classes/:id` (catálogo y reserva, `canManage` siempre `false`), `/portal/reservations` (propias, cancelables), `/portal/profile` (comparte `ProfileContent` con `/dashboard/profile`).
  - `proxy.ts`: `/portal` es CLIENT-only (`rolePermissions['/portal'] = ['CLIENT']`); cualquier acceso de CLIENT a `/dashboard/*` redirige a `/portal` (chequeo previo al loop de permisos) — CLIENT ya no tiene ninguna ruta dentro de `/dashboard` (se sacó de `rolePermissions['/dashboard/classes']` y `['/dashboard/reservations']`, y se borró la entrada de `/dashboard/membership`). `login/page.tsx` redirige a `/portal` si `data.user.role === 'CLIENT'`, a `/dashboard` en cualquier otro caso.
  - Componentes compartidos entre `/dashboard` y `/portal` para no duplicar lógica (mismo patrón `Section` ya establecido, aplicado ahora también a nivel de página completa, no solo de sub-sección): `ClassDetailContent` (extraído de la vieja `/dashboard/classes/[id]/page.tsx`, props `classesHref`/`reservationsHref` para que cada superficie arme sus propios links), `ReservationsContent` (extraído de la vieja `/dashboard/reservations/page.tsx`, props `title`/`subtitle`/`canCancel`/`newReservationHref`), `ProfileContent` (extraído de `/dashboard/profile/page.tsx`, sin props — usa `router.back()`, funciona igual en ambos contextos). `ClassTable`/`ClassesSection` ahora aceptan `detailBasePath` opcional (default `/dashboard/classes`) para que el link "Ver disponibilidad" apunte a la superficie correcta.
  - `sidebar.tsx`: se sacó `CLIENT` de los roles de `Clases`/`Reservas` y se borró la entrada de `Mi Membresía` — el sidebar de `/dashboard` ya no le muestra nada a CLIENT (no importa, nunca lo ve).
  - Verificado en vivo con fixture temporal (gym existente + plan + membership activa + invoice + reservation, vía SQL directo ya que no hay tráfico real de Mercado Pago): login como CLIENT redirige a `/portal`; Mi Membresía muestra plan/estado/historial de facturas; Clases → detalle → Reservar decrementa cupo y confirma; Mis Reservas lista ambas reservas y cancela una (status pasa a "Cancelada", desaparece el botón); Mi Perfil funciona; `/dashboard` con cookie `CLIENT` redirige a `/portal`; `/portal` con cookie no-CLIENT redirige a `/unauthorized`. Fixture eliminado después de verificar.
  - **Rediseño de `ProfileContent`, 2026-07-20** — feedback real del usuario probando el portal ("mejorame el diseño... algo nuevo en 3D"), sesión de scoping corta: profundidad/vidrio con CSS puro, no un elemento 3D real (sin Three.js). Nuevo primitivo reusable `src/components/ui/TiltCard.tsx` (inclina la tarjeta siguiendo el mouse vía `perspective`/`rotateX`/`rotateY`, sin librerías) — usalo si un componente nuevo pide este mismo efecto, no lo reimplementes. Fondo con degradado + blobs difuminados (`blur-2xl`, no `blur-3xl`: con `blur-3xl` + `backdrop-blur` en varias tarjetas que además tienen `transform` de `TiltCard`, el compositing se puso pesado — se sacó `backdrop-blur` de las tarjetas por completo, quedan traslúcidas (`bg-white/85`) sin sampling del fondo, mucho más barato). Como `ProfileContent` es compartido, el rediseño aplica igual a `/dashboard/profile` y `/portal/profile` sin tocar ninguna de las dos páginas.
  - **Mismo rediseño extendido a todo `/portal`, 2026-07-20** — pedido explícito de seguir con "el resto del portal también en 3D, diseño similar a todo". Mismo fondo (degradado + blobs) copiado en cada página de `/portal` (no se centralizó en `PortalLayout` a propósito, para no duplicar el fondo en `/portal/profile`, que ya trae el suyo propio dentro de `ProfileContent`). Nuevo `PortalPageHero` (`src/components/portal/PortalPageHero.tsx`) — mismo hero con gradiente/insignia que "Mi Perfil", con un slot `action` opcional para un botón a la derecha (lo usa `/portal/reservations` para "+ Nueva reserva"). Las tarjetas ya existentes (`MembershipStatusCard`, `InvoiceTable`, `ClassesSection`, `ReservationsContent`) se envuelven en `TiltCard` **sin tocar su estilo interno** — son compartidas con `/dashboard`, así que solo se les agrega el wrapper de transform, nunca se les cambia la clase de fondo/borde propia, para no afectar cómo se ven ahí. `ReservationsContent` ganó un prop `hideHeader` (default `false`) para que `/portal/reservations` use su propio `PortalPageHero` en vez de duplicar el título — `/dashboard/reservations` no pasa este prop, sigue exactamente igual. `PortalNav` ahora es `sticky` con un `backdrop-blur-md` liviano — es un elemento estático (no tiene `transform` de `TiltCard` corriendo en cada mousemove), así que el `backdrop-blur` ahí no repite el problema de rendimiento que sí tuvo `ProfileContent`.
  - **Identidad visual reemplazada por completo — "gimnasio boutique industrial", 2026-07-20** — el gradiente indigo/púrpura de los dos puntos anteriores quedó **superado** (no lo seas fiel si aparece en código viejo o en una captura vieja): el usuario mandó fotos reales de un gimnasio ("Gym Privado" — negro mate, paneles de madera, letrero blanco pesado en mayúsculas con una línea de luz fría debajo) y pidió llevar esa estética a **toda la app**, no solo al portal. Se implementó como un re-skin completo vía tokens, no rediseñando componente por componente:
    - Paleta nueva en `src/app/globals.css`, vía `@theme` de Tailwind v4 (colores con nombre propio, no se pisó la paleta built-in de Tailwind): `ink-950/900/850/800/700` (fondo de página → panel → superficie elevada, de más oscuro a menos), `wood-700/600/500/400` (acento estructural — nav activo, avatar, badges, botones primarios), `neon-500/400/300` (el único acento "eléctrico": links y el detalle de línea bajo los títulos, a propósito no se usa en más lugares), `cream`/`cream-muted`/`cream-faint` (texto), y `success`/`warn`/`pop` (estados semánticos, con su propio `-bg` ya resuelto en rgba en vez de depender del modificador de opacidad de Tailwind). `body` en `globals.css` fija el fondo/color por defecto. ~~Un solo tema, sin modo claro, a propósito~~ — **superado el mismo día, ver el punto de `ThemeToggle` más abajo**: el usuario probó el re-skin y no le gustó no tener opción, pidió explícitamente un selector.
    - Migración ejecutada con un script de reemplazo literal (no regex) sobre todos los `.tsx` de `src`, mapeando cada clase vieja de Tailwind (`slate-*`, `indigo-*`, `purple-*`, `emerald-*`, `amber-*`, `red-*`, `blue-*`, `green-*`, `teal-*`, y los distintos `bg-white`/`text-white`/`border-white/N`) a su equivalente nuevo — cubrió 49 de ~55 archivos con componentes/páginas en un solo pase. Si agregás una clase de color nueva en un archivo, usá los tokens de arriba directamente (`bg-ink-850`, `text-cream`, etc.) — no reintroduzcas `slate`/`indigo`/`white` sueltos, que ya no combinan con el resto.
    - Gaps reales que dejó el script automático y que se corrigieron a mano después: los gradientes de tres paradas tipo `from-indigo-50 via-white to-purple-50` quedaban con un `via-white` sin tocar (destello blanco en medio de un fondo oscuro) — se reemplazaron por un `bg-ink-950` plano en los 5 archivos que tenían ese patrón (`ProfileContent` y las 4 páginas de `/portal`). También quedaron sueltos algunos `text-blue-600` (links en `login`/`reset-password`/`signup-gym`, un color que el script no cubría porque no aparecía en el primer grep de auditoría), `border-l-teal-500`/`bg-green-50`/`border-green-200`/`text-green-600` (un módulo "Próximamente" del dashboard y los banners de éxito de `register`/`forgot-password`/`reset-password`). Antes de dar por terminado un re-skin así, correr `grep -rohE` por familia de color después del script y no solo confiar en la lista inicial — hay usos que no aparecen hasta que grepeás la familia exacta (`blue`, `teal`, `green` no estaban en el barrido inicial de `slate|indigo|purple|emerald|amber|red`).
    - `Sidebar` (`/dashboard`) y `PortalNav` (`/portal`) reescritos a mano (no por el script) para el tratamiento real del mockup: degradado de fondo `ink-900`→casi negro, ítem activo con borde izquierdo `wood-500` (no un bloque de color sólido), avatar circular con borde `wood-500` e inicial, "kicker" en mayúsculas `wood-500` para el rol. `Button`/`Input`/`ConfirmationModal` (los primitivos compartidos en `src/components/ui/`) también a mano, ya que son la base de todos los formularios/modales de la app.
    - `TiltCard` ganó un prop `style?` opcional (se mergea con su propio `transform`/`transition`, nunca lo pisa) — lo necesitó `PortalPageHero` para agregar una sombra extra sin duplicar el componente.
    - Verificado en vivo con SUPER_ADMIN de prueba (creado y borrado por SQL, mismo patrón que sesiones anteriores) y con `socio.demo@smartbox.test`: login, dashboard home, Gimnasios (listado + detalle + modal "Nueva clase"), Usuarios, y las cuatro páginas de `/portal` — todas coherentes con la paleta nueva, sin clases de color viejas sobrevivientes (confirmado con un grep final por las 9 familias de color reemplazadas, sin resultados). `npx tsc --noEmit`, `npm run build` y `npm run lint` limpios (el lint no sumó problemas nuevos vs. el baseline anterior al re-skin).
  - **`ThemeToggle` — selector claro/oscuro, implementado 2026-07-20 (mismo día que el re-skin de arriba)** — el usuario probó el "madero" único y pidió explícitamente poder elegir: oscuro = madero, claro = "el que tenía antes" (el esquema slate/indigo previo a este mismo día, no el intento de portal en índigo/púrpura que quedó superado).
    - Mecanismo: cada color de `@theme` en `globals.css` se declara dos veces como variable **cruda** `--t-*` (una en `:root`, otra bajo `:root[data-theme="light"]`), y el bloque `@theme` de Tailwind solo la referencia (`--color-cream: var(--t-cream)`). Una sola clase de Tailwind (`bg-ink-850`, `text-cream`, etc.) sirve para los dos temas sin tocar ningún componente — cambiar de tema es solo pisar el atributo `data-theme` en `<html>`.
    - **Trampa real de CSS con la que hay que tener cuidado si se toca este sistema**: las custom properties de Tailwind (`--color-cream`) quedan "resueltas" una sola vez en el elemento donde se declaran (`:root`), sustituyendo `var(--t-cream)` por el valor que tenía `--t-cream` *en ese mismo elemento* en ese momento. Redefinir `--t-cream` en un descendiente (ej. `.sidebar-scope`) **no** hace que `--color-cream` vuelva a resolverse ahí — el descendiente simplemente hereda el valor ya calculado en `:root`. Para overridear color en un scope puntual hay que pisar el nombre final `--color-*`, no el `--t-*` intermedio (ver `.sidebar-scope` en `globals.css` para el patrón correcto). Cualquier "excepción de tema" nueva (un elemento que deba quedarse fijo en un color pase lo que pase) tiene que seguir este mismo patrón.
    - `Sidebar` (`/dashboard`) es oscuro **en los dos temas a propósito** — así era la app original (contenido claro, sidebar siempre oscuro, nunca hubo una versión clara de él). `--t-sidebar-top`/`--t-sidebar-deep` (el degradado de fondo) solo se declaran en `:root`, sin override en `:root[data-theme="light"]`, así que quedan fijos. `.sidebar-scope` (aplicada al `<aside>`) fija además `--color-cream`/`-muted`/`-faint`, `--color-ink-line`/`-strong` y `--color-pop`/`-bg` para que el texto siga siendo legible sobre el fondo oscuro fijo, sin importar el tema — **pero no fija `--color-wood-*`**, a propósito: así el acento del sidebar (ítem activo, avatar, badges) sigue siendo madera en tema oscuro e índigo en tema claro, coherente con lo que el usuario eligió, aunque el fondo del sidebar no cambie. `PortalNav` (`/portal`) es la superficie inversa — no tiene una versión "original" que preservar (es nueva de `E6-02`), así que sí seguía el tema completo desde el principio; solo tenía un `rgba(...)` de fondo hardcodeado que no flippeaba — se movió a `--t-nav-glass` (blanco translúcido en claro, negro translúcido en oscuro).
    - `ThemeToggle` (`src/components/ui/ThemeToggle.tsx`) — botón simple, persiste en `localStorage` (`smartbox-theme`), sin dependencias nuevas. Colocado en el pie de `Sidebar`, **también en el header de `/dashboard`** (al lado del badge de rol — el usuario pidió más visibilidad ahí, no alcanzaba con tenerlo solo en el pie del sidebar), en `PortalNav`, y en las 6 páginas de auth sin nav propio (`login`, `register`, `forgot-password`, `reset-password`, `signup-gym`, `unauthorized`) como un botón flotante arriba a la derecha. Si se agrega un nuevo layout/página de auth, replicar ese mismo patrón (botón flotante `absolute top-5 right-5`) en vez de inventar uno nuevo. Sin flash del tema equivocado al cargar: `layout.tsx` inyecta un `<Script strategy="beforeInteractive">` que lee `localStorage` y aplica `data-theme="light"` en `<html>` *antes* del primer paint (oscuro es el default sin atributo, así que el script no necesita hacer nada si el usuario nunca eligió claro) — `suppressHydrationWarning` en `<html>` porque este script muta el DOM fuera del control de React antes de la hidratación.
    - **Tono de madera aclarado, 2026-07-20** — feedback directo del usuario tras ver el primer resultado ("el color madera hazme más un tono más claro"). Se corrió toda la rampa `--t-wood-700/600/500/400` un escalón hacia arriba (cada valor pasó a ser el que tenía el escalón de abajo, y se agregó un `400` nuevo más claro todavía) — el acento principal (`wood-600`, botones primarios/nav activo) pasó de `#8a5c3a` a `#a8734a`. Solo se tocó el bloque `:root` (tema oscuro/madero) — los tonos índigo del tema claro no cambiaron, "madera" no existe ahí.
  - **Revisión del flujo de navegación, 2026-07-20 (pedido explícito junto con el toggle)** — se corrigió la bajada de `/dashboard/gyms` ("alta manual, sin onboarding self-serve todavía"), que quedó desactualizada desde `E6-05` (ya existe `/signup-gym`). Hallazgo real, resuelto el mismo día (ver el punto siguiente): `/register` no tenía ningún link entrante desde ningún lado de la app, y el formulario no pedía `gymId` aunque el backend ya lo exigía desde Epic 1 — la página estaba efectivamente rota. No se agregó el link a `/login` en el momento del hallazgo a propósito (habría expuesto el bug), sí se agregó después de arreglar el formulario.
  - **`/register` arreglada — selector de gimnasio, 2026-07-20** — el backend (`AuthService.register()`) ya exigía `gymId` desde Epic 1, pero nunca hubo forma pública de listar gimnasios para elegir uno (`GET /gyms` es `SUPER_ADMIN`-only) — se agregó `GET /auth/gyms` (ver `CLAUDE.md` del backend) devolviendo `{id, name}[]`. `/register` ahora carga ese listado al montar y agrega un `<select>` "Gimnasio al que te unís" (preseleccionado con el primero, deshabilitado si `loadingGyms` o si no hay ningún gimnasio) — el botón "Registrarse" queda deshabilitado y se muestra un aviso si `gyms.length === 0`, en vez de dejar enviar un formulario que el backend va a rechazar. `POST /auth/register` ahora manda `gymId` en el body. Con esto arreglado, se agregó el link "¿Sos socio de un gimnasio? Registrate" en `/login`, al lado del de `/signup-gym` — ya no hay razón para no ser descubrible. Verificado en vivo end-to-end contra el backend real: alta de un socio con el gym real de la base de datos, `gym_id` correcto confirmado por SQL, fixture borrado después.
- **`ThemeToggle` simplificado + fondo claro corregido + voseo→tuteo, 2026-07-21** — feedback directo del usuario en `/login`: el botón mostraba "🌲 Modo madero" (confuso, mezcla branding con la acción del control) y el tema claro se leía "todo blanco" porque `--t-ink-950`/`--t-ink-850` eran casi idénticos (`#f8fafc` vs `#ffffff`, sin jerarquía visual entre fondo de página y tarjeta).
  - `ThemeToggle` ahora solo dice "Modo oscuro"/"Modo claro" (sin "madero") — la lógica de toggle no cambió, solo el texto del botón.
  - Tema claro: `--t-ink-950` pasó a `#e8edfb` (fondo de página con tinte índigo visible) y `--t-ink-900`/`-800`/`-700`/`-line`/`-line-strong` se ajustaron en la misma dirección; `--t-ink-850` (tarjetas) se mantuvo `#ffffff` — el contraste blanco-sobre-azul es intencional, así se veía la app original (`bg-slate-50`+`indigo-600` de antes del re-skin), no un blanco plano.
  - **Voseo → tuteo en toda la app** (pedido explícito: "el texto en ecuador seria eres dueño de un gimnasio", Ecuador usa tuteo, no voseo rioplatense) — se corrigieron todas las instancias encontradas por grep (`sos`→`eres`, `podés`→`puedes`, `tenés`→`tienes`, `creá`/`intentá`/`conectá`/`seleccioná`/`dá`→`crea`/`intenta`/`conecta`/`selecciona`/`da`, etc.) en `login`, `register`, `signup-gym`, `unauthorized`, `dashboard/settings`, `MembershipStatusCard`, `PlansSection`, `ReservationTable`, `CreateShiftModal`. Si se agrega copy nueva, usar tuteo (`tú`/`eres`/`tienes`), nunca voseo (`vos`/`sos`/`tenés`) — la app es para Ecuador.
- **Acento marrón/"madero" del tema oscuro reemplazado por azul-morado, 2026-07-21** — feedback directo del usuario tras ver el sidebar/dashboard: "por contraste esta feo ese color madera, quitalo", pidiendo blanco + azul-morado en su lugar. Se cambiaron únicamente los valores de `--t-wood-700/600/500/400` en `:root` (tema oscuro) de la rampa marrón (`#8a5c3a`→`#d9b28c`) a una rampa índigo/violeta (`#4338ca`→`#a5b4fc`), coherente con el acento índigo que el tema claro ya tenía. **Se mantuvo el nombre de la variable `--t-wood-*`/clases `bg-wood-*`/`text-wood-*`/`border-wood-*`** — cambiar el nombre hubiera significado tocar ~25 archivos que ya referencian esas clases (botones primarios, nav activo del sidebar, badges, avatares) sin ganar nada funcional; "wood" quedó como nombre histórico del token de acento estructural, ya no describe un color marrón. El tema claro no se tocó (ya era índigo desde el re-skin). Verificado en vivo con una sesión simulada (`localStorage` con un usuario `SUPER_ADMIN` de mentira, sin login real) en `/dashboard`: sidebar y header con acento azul-morado consistente en los dos temas, sin rastro de marrón.
- **Contraste tarjeta/fondo del tema oscuro + `ThemeToggle` en posición única, 2026-07-21 (mismo día, feedback siguiente)** — dos pedidos sobre un screenshot real de `/login` en oscuro: (1) "todo esta oscuro", la tarjeta (`--t-ink-850`) casi no se distinguía del fondo de página (`--t-ink-950`) — la diferencia entre ambos era de solo ~8 puntos por canal (`#121110` vs `#1a1816`); (2) "dejame en el mismo sitio del login en todas las pantallas", el botón de tema estaba duplicado y en tres posiciones distintas según la pantalla (flotante `absolute top-5 right-5` en las 6 páginas de auth sin nav propia, embebido en el pie de `Sidebar`, en el header de `/dashboard`, en la nav de `PortalNav`).
  - Contraste: se separó toda la rampa `--t-ink-950/900/850/800/700` (tema oscuro) para un salto real entre fondo de página, tarjeta y superficies elevadas (`#0b0a0c`→`#121016`→`#1f1c24`→`#29262f`→`#37333d`). El tema claro no se tocó.
  - Posición única: `ThemeToggle` ahora se renderiza **una sola vez**, en `layout.tsx` (raíz, dentro de `<AuthProvider>`, fuera de `{children}`), como `fixed top-5 right-5 z-50` con fondo propio opaco (`bg-ink-850`) para leerse encima de cualquier contenido de cualquier pantalla — antes no tenía fondo propio porque flotaba solo sobre el `bg-ink-950` uniforme de las páginas de auth, insuficiente ahora que también flota sobre `Sidebar`/`PortalNav`/contenido de página. Se **eliminaron** las 8 instancias por-página/componente que existían antes (los 6 `<div className="absolute top-5 right-5">` de las páginas de auth, el botón del pie de `Sidebar`, el del header de `/dashboard`, el de la nav de `PortalNav`) — si se agrega una pantalla nueva (auth, dashboard o portal), **no** volver a agregar un `ThemeToggle` local, ya viene de layout.tsx.
  - Verificado en vivo (`tsc`/`build`/`lint` limpios, mismo baseline de 22 issues preexistentes) en `/login` (oscuro y claro) y `/dashboard` (con una sesión `SUPER_ADMIN` simulada vía `localStorage`, sin login real) — botón en el mismo punto exacto de la pantalla en ambos casos, tarjetas con contraste visible contra el fondo.
- **`CreateUserModal` cableado a `POST /users` en vez de `POST /auth/register-internal`, 2026-07-21** — encontrado en una auditoría completa pedida por el usuario: `register-internal` es `SUPER_ADMIN`-only en el backend, así que un `ADMIN` que hacía clic en "Crear Usuario" desde `/dashboard/users` recibía un 403 silencioso (el modal solo mostraba el mensaje de error genérico). `POST /users` sí acepta `ADMIN` (fuerza `gymId` al propio gimnasio del lado del backend) — el fix fue puramente cablear el modal ahí, no crear nada nuevo. Cambio de contrato del body: `roleName` (string) → `roleId` (UUID) — el `<select>` de rol ahora usa `r.id` como `value` en vez de `r.name`. El fix real (`UsersService.create()` no hasheaba la contraseña) vive en el backend, ver `CLAUDE.md` de `smartbox-backend`. Verificado en vivo con el flujo completo real (sin fakear sesión): `/signup-gym` → login automático como `ADMIN` → `/dashboard/users` → "Crear Usuario" → alta de un `STAFF`, aparece en la tabla sin error.
- **Selector de gimnasio en `CreateUserModal` para `SUPER_ADMIN`, 2026-07-21 (mismo día, pedido de seguimiento explícito: "arreglá el flujo de superadmin")** — el fix anterior dejó un gap: `SUPER_ADMIN` (sin `gymId` propio) usando el mismo modal para crear un `ADMIN`/`STAFF`/`CLIENT` seguía fallando con "gymId es obligatorio", porque el modal nunca pedía/enviaba `gymId` y `/dashboard/users` no tiene contexto de gimnasio para ese rol (a diferencia de `GymDetailPage`, que sí lo tiene). Se agregó un `<select>` de gimnasio (`GET /gyms`, ya `SUPER_ADMIN`-only en el backend, mismo endpoint que ya usa `/dashboard/gyms`) que aparece **solo** cuando `user.role === 'SUPER_ADMIN'` y el rol elegido en el formulario no es `SUPER_ADMIN` — para `ADMIN` (el backend ya le fuerza su propio `gymId`) o para `SUPER_ADMIN` creando otro `SUPER_ADMIN` (no necesita gimnasio), el formulario se ve exactamente igual que antes, sin selector. Verificado en vivo end-to-end con un JWT `SUPER_ADMIN` real (firmado a mano con el mismo `JWT_SECRET` del backend, ya que no hay credenciales de un `SUPER_ADMIN` de prueba disponibles): el selector aparece/desaparece correctamente al cambiar el rol elegido, se puebla con el gimnasio real (`GET /gyms` contra la base real), y el usuario creado queda con el `gymId` correcto confirmado por SQL. `tsc`/`build`/`lint` limpios, mismo baseline de 22 issues preexistentes.
- **Sin GET /plans?gymId, GET /classes?gymId, GET /shifts?gymId, ni listado admin de membresías/facturas en el backend** — `GymDetailPage` (SUPER_ADMIN), `PlansSection`, `ClassesSection` y `ShiftsSection` piden la lista completa (`GET /plans`/`GET /classes`/`GET /shifts`) y filtran por `gymId` en el cliente, aceptable mientras haya pocos gimnasios en v1.0; no hay forma de que un ADMIN vea el historial de facturas de otro socio (E2-07 es deliberadamente autogestión de solo lectura, sin endpoint admin-side).
- **`PlansSection`/`ClassesSection`/`ShiftsSection`/`ReportsSection` son el mismo patrón**: un componente compartido con props `{gymId, canManage}` (o `canViewRevenue`/`mercadoPagoConnected` según el módulo), embebido tanto en `GymDetailPage` (SUPER_ADMIN, siempre con permiso total) como en su propia página bajo `/dashboard/<módulo>` (ADMIN/STAFF/CLIENT según corresponda, `gymId` resuelto de `user.gymId`) — no dupliques la lógica de fetch/filtro si sumás un módulo con esta misma forma (un catálogo o vista por gimnasio que SUPER_ADMIN gestiona desde el gimnasio y el resto de roles desde su propia sección). `PlansSection` reemplazó a un `PlanCard` más viejo (pensado para un solo plan por gimnasio) cuando `E6-04` sumó múltiples planes — si ves referencias a `PlanCard` en memoria/docs viejos, es el nombre anterior de este mismo componente.
- **`POST /memberships/subscribe` ahora exige `{ planId }` en el body** (antes no recibía body, resolvía "el" plan del gimnasio solo) — `MembershipStatusCard` le muestra al socio una lista de planes (radio buttons) antes de habilitar "Suscribirme"; sin plan seleccionado, el botón queda deshabilitado. Si `plans` llega vacío (gimnasio sin planes configurados), se muestra un mensaje en vez del picker.
- **`POST /shifts` no devuelve la relación `staff`** (a diferencia de `GET /shifts`, que sí la trae) — `ShiftsSection.onSuccess` completa el nombre a mano desde `staffOptions` (ya cargado para el selector del modal) en vez de pedirle al backend que la incluya, para no tener que re-fetchear toda la lista tras cada alta. Si un patrón similar aparece en un módulo nuevo (alta que no devuelve una relación que la tabla necesita para pintarse), preferí este mismo truco de completar client-side antes que forzar un round-trip extra.
- **Bugs reales encontrados y corregidos en el backend durante estas sesiones**: `MembershipsService.requestCancellation()`, `ReservationsService.cancel()` (2026-07-18) y `ShiftsService.findAll()` para SUPER_ADMIN (2026-07-20) — los dos primeros volvían a buscar la entidad sin la relación (`plan`/`classOrResource`) después de actualizar el status, y el tercero no traía `staff.gym` en la rama de SUPER_ADMIN, rompiendo el filtro por `gymId` en `ShiftsSection`. Si agregás un `cancel()`/`requestX()`/`findAll()` nuevo con este patrón, **acordate de las `relations`** que el frontend necesita para pintar o filtrar la respuesta.
- **`/confirm-email` creada — el flujo de cambio de email estaba roto de punta a punta, 2026-07-21 (pedido explícito de seguimiento)** — el correo de verificación (backend, `MailService.sendEmailChangeVerification()`) linkea a `${FRONTEND_URL}/confirm-email?token=...`, pero esa ruta nunca existió en este repo. Había un intento previo, `/dashboard/verify-email/page.tsx`, que **nunca llegó a commitearse** (no aparece en `git log`) y estaba doblemente roto: usaba `useParams()` esperando un segmento dinámico `[token]` que no era una carpeta real (nunca resolvía el token), y llamaba a `GET /auth/verify-email-change`, que además se eliminó del backend por ser un duplicado exacto de `POST /users/confirm-email-change` (ver `CLAUDE.md` del backend). Se creó `/confirm-email/page.tsx` siguiendo el mismo patrón que `/reset-password` (`Suspense` + `useSearchParams()`, no `useParams()`), llamando a `POST /users/confirm-email-change` con `{ token }` en el body. Verificado en vivo end-to-end contra el backend y la base de datos reales: solicitud real de cambio de email (`ProfileContent` → `POST /users/request-email-change`), token real extraído de la base, navegación real del navegador a `/confirm-email?token=...` con confirmación exitosa (email actualizado, `pending_email`/token limpiados en la base), y el caso de token inválido mostrando "Token inválido o expirado." en vez de quedarse colgado. `tsc`/`build`/`lint` limpios — de hecho el lint bajó de 22 a 21 problemas preexistentes, porque la página vieja rota tenía su propio warning de React que se fue con ella. Fixtures de prueba borrados después.
- **`Sidebar`: links rotos y dead-end de `SUPER_ADMIN` corregidos, 2026-07-21 (pedido explícito, "sigamos")** — dos problemas del mismo componente encontrados en la auditoría del 2026-07-21, resueltos juntos:
  - **3 links a páginas que nunca existieron** (`/dashboard/pods`, `/dashboard/payments`, `/dashboard/logs` — 404 real). Se agregó `comingSoon: true` a esos tres items de `menuItems`; cuando está presente, `Sidebar` los renderiza como un `<div>` no interactivo (`opacity-60 cursor-not-allowed`, sin `<Link>`) con un badge "Pronto" — mismo lenguaje visual que las tarjetas "Próximamente" que ya existían en `/dashboard/page.tsx`. Si agregás un módulo real para alguno de estos tres, sacale `comingSoon` y va a volver a comportarse como un link normal sin tocar nada más.
  - **`SUPER_ADMIN` con un callejón sin salida en Clases/Turnos de Staff/Reportes/Configuración** — las cuatro páginas resuelven `gymId` desde `user.gymId`, que es `null` para `SUPER_ADMIN` (muestran "tu cuenta no está asociada a ningún gimnasio"). Se sacó `'SUPER_ADMIN'` del array `roles` de esos cuatro items — la función real para ese rol ya existía (sin cambios) entrando por `/dashboard/gyms/[id]` (`GymDetailPage` embebe `ClassesSection`/`ShiftsSection`/`ReportsSection`/`PlansSection` con `gymId` explícito). `Reservas` no tenía este problema (`ReservationsService.findAll()` ya tenía una rama explícita para `SUPER_ADMIN` sin filtrar por gym) y se dejó igual.
  - Verificado en vivo con sesiones `SUPER_ADMIN` y `ADMIN` reales (JWT firmados a mano con el `JWT_SECRET` del backend, sin credenciales de prueba disponibles): el sidebar de `SUPER_ADMIN` ya no muestra los 4 links de gym-scope, y clic en cualquiera de los 3 "Pronto" no navega a ningún lado; el sidebar de `ADMIN` no cambió, conserva los 7 módulos operativos con acceso completo. `tsc`/`build`/`lint` limpios, mismo baseline de 21 issues preexistentes.
- **`/dashboard/admin` eliminada, 2026-07-21 (pedido explícito de seguimiento, cierra la auditoría del 2026-07-21)** — página huérfana sin ningún link entrante en toda la app (solo alcanzable tipeando la URL a mano), duplicado placeholder abandonado de `/dashboard/users` (que ya es la versión real y funcional). `UserList`/`RoleManager` (`src/components/admin/`) eran componentes vacíos ("Aquí cargarás la tabla de usuarios próximamente" / "Aquí gestionarás los permisos y roles", sin un solo `apiFetch`) usados únicamente por esta página. Se eliminaron los tres archivos (`page.tsx`, `UserList.tsx`, `RoleManager.tsx`) y la entrada `'/dashboard/admin'` en `proxy.ts` (quedó muerta, sin ninguna ruta que proteger). **Gotcha de verificación**: borrar `.next/` a mano mientras el server de dev (Turbopack) sigue corriendo corrompe su cache persistente (`Failed to restore task data`) — hay que parar el proceso antes de tocar `.next/`, no solo regenerarlo con `npm run build` en paralelo. Verificado en vivo tras reiniciar el dev server limpio: `/dashboard/admin` ahora redirige a `/login` (ruta protegida sin token) en vez de servir el placeholder o tirar un error; `/dashboard/users` sin cambios, sigue siendo la única fuente de verdad. `tsc`/`build`/`lint` limpios, mismo baseline de 21 issues preexistentes.
- **Undertone marrón del fondo del sidebar en oscuro, corregido 2026-07-21** — el fix del acento "madero" (arriba, mismo día) cambió `--t-wood-*` a azul-morado pero no tocó `--t-sidebar-top`/`--t-sidebar-deep` (el gradiente de fondo del `<aside>`), que seguían en `#17110c`/`#0e0c0b` — valores con `R>G>B`, un marrón real, no solo percibido. El usuario lo siguió viendo como "color madera" porque el fondo, no el acento, era lo marrón. Se cambiaron a `#121016`/`#08080b` (neutros con leve tinte azulado, coherentes con la rampa `--t-ink-*` ya neutralizada en el fix de contraste anterior). Verificado en vivo con una sesión `ADMIN` real (JWT firmado a mano): sidebar sin ningún undertone cálido, el acento azul-morado sigue intacto (nav activo, badges, avatar).
- **Modo oscuro: de negro mate a gris plateado, 2026-07-21 (mismo día, feedback siguiente)** — el usuario pidió explícitamente "un color más claro, un plateado" para el modo oscuro; se le preguntó si aplicaba solo al sidebar o a todo el tema y eligió **todo**. Se relevantó toda la rampa `--t-ink-950/900/850/800/700` (fondo de página → tarjeta → superficies elevadas) de negro casi puro (`#0b0a0c`) a un gris plateado (`#24262b` como base, con la misma progresión de contraste del fix anterior) y `--t-sidebar-top`/`-deep` a la misma familia (`#2c2e34`/`#1c1e22`), para que el sidebar deje de ser un negro más oscuro que el resto y quede en el mismo tono plateado. `--t-nav-glass` (fondo translúcido de `PortalNav`) también se actualizó a juego. El acento `--t-wood-*` (azul-morado) y el resto de la paleta (`--t-neon-*`, semánticos) no cambiaron. El tema claro no se tocó. Verificado en vivo con una sesión `ADMIN` real: `/login` y `/dashboard` (sidebar + contenido) en gris plateado consistente, con la jerarquía de contraste tarjeta/fondo del fix anterior todavía visible. `tsc`/`build`/`lint` limpios, mismo baseline de 21 issues preexistentes.
- **`UserTable`: columna "Gimnasio" agregada, 2026-07-21** — `GET /users` ya devolvía la relación `gym` completa (`UsersService.findAll()`, sin cambios de backend necesarios), pero `UserTable` nunca la pintaba — no había forma de saber de qué gimnasio era cada usuario en la tabla, relevante sobre todo para `SUPER_ADMIN` (ve usuarios de todos los gimnasios mezclados en una sola lista). Se agregó la columna (`user.gym?.name`, `"—"` para `SUPER_ADMIN`, que no pertenece a ningún gimnasio) y se tipó `gym.name` en `User` (`types/index.ts`, antes solo tenía `gym?: {id}`). Verificado en vivo con una sesión `SUPER_ADMIN` real y dos gimnasios reales (uno de prueba, borrado después): cada fila muestra el gimnasio correcto, la fila de `SUPER_ADMIN` muestra "—". `tsc`/`build`/`lint` limpios, mismo baseline de 21 issues preexistentes.
- **Filtros en Gestión de Usuarios (búsqueda, rol, gimnasio), 2026-07-21 (pedido explícito, "son muy importantes")** — `GET /users` siempre trae la lista completa sin paginación; sin filtros, se vuelve inmanejable apenas hay más de unos pocos usuarios (mismo problema que motivó la columna "Gimnasio" arriba). Filtrado 100% client-side en `UsersPage` (`useMemo` sobre el `users` ya cargado, sin round-trip nuevo por cada cambio) — búsqueda por nombre/email (case-insensitive, substring), selector de rol (usa los roles reales de `GET /roles`, no una lista hardcodeada) y selector de gimnasio (derivado de los `gym.id`/`gym.name` únicos presentes en la lista cargada, no de `GET /gyms`, que es `SUPER_ADMIN`-only — evita el problema de permisos y automáticamente refleja solo los gimnasios que el requester puede ver). **El selector de gimnasio se auto-oculta** (`gymOptions.length > 1`) cuando solo hay un gimnasio en los datos — que es siempre el caso para `ADMIN`/`STAFF` (ya ven solo su propio gym filtrado server-side), así que no hace falta leer el rol del usuario actual para decidir si mostrarlo. `UserTable` gana un prop opcional `emptyMessage` para distinguir "no hay usuarios" de "ningún usuario coincide con los filtros" (colspan del `<tr>` vacío ajustado de 4 a 5 cuando se agregó la columna Gimnasio, ver punto anterior). Verificado en vivo con una sesión `SUPER_ADMIN` real y dos gimnasios (uno de prueba, borrado después): el selector de gimnasio filtra correctamente (2 de 5 registros al elegir uno), la búsqueda por texto matchea contra nombre/email, y el estado vacío por filtros muestra el mensaje correcto en vez del genérico. `tsc`/`build`/`lint` limpios, mismo baseline de 21 issues preexistentes.
- **Filtros extendidos a Reservas y Gimnasios, 2026-07-21 (mismo día, "añademe esos filtros a cada sección que requiera")** — antes de programar se hizo un inventario de todas las listas/tablas del panel para decidir dónde realmente aportan valor (no se agregaron a ciegas en todos lados):
  - **Reservas** (`ReservationsContent`, compartido sin cambios por `/dashboard/reservations` y `/portal/reservations`) — la sección más urgente: confirmado que `GET /reservations` no tiene paginación ni filtro de fecha en el backend, devuelve el historial completo sin tope (`ReservationsService.findAll()`, ordenado por `startAt DESC`, sin `where` de fecha). Se agregó búsqueda por nombre de clase, selector de estado (reusa `STATUS_LABEL`, ahora exportado desde `ReservationTable.tsx`, para no duplicar los labels `confirmed`/`cancelled`/`expired` → `Confirmada`/`Cancelada`/`Vencida`) y rango de fechas (`from`/`to` sobre `startAt`, inputs `type="date"`). Mismo patrón client-side que Usuarios — el backend no tiene ningún query param de fecha que aprovechar todavía, así que no hay nada que reusar del lado servidor.
  - **Gimnasios** (`/dashboard/gyms`, SUPER_ADMIN) — el otro dataset que crece sin límite real (son los tenants/clientes del SaaS, más los que se auto-registran vía `/signup-gym`). Búsqueda por nombre/dirección y selector de estado de conexión a Mercado Pago (conectado/sin conectar, ya visible como badge en la tabla — el filtro solo lo hace accionable).
  - **Deliberadamente sin filtros**: Clases (`ClassTable`), Turnos de Staff (`ShiftTable`), Planes (`PlanTable`) e Historial de facturas del portal (`InvoiceTable`) — son listas acotadas por diseño (recurrencias semanales por gimnasio, o pocos ítems por gym/usuario), no crecen sin límite como Reservas o Gimnasios. Agregarles filtros sería complejidad sin beneficio real medible — si el dataset de alguna de estas crece mucho más de lo esperado en el futuro, revisar esta decisión, no asumir que ya están cubiertas.
  - Ambas tablas ganan el mismo prop `emptyMessage` que `UserTable` (ver punto anterior), mismo patrón de "Limpiar filtros" condicional. Verificado en vivo con datos reales (sesión `ADMIN` para reservas, `SUPER_ADMIN` para gimnasios): el filtro de estado en reservas y el de nombre/Mercado Pago en gimnasios funcionan correctamente. `tsc`/`build`/`lint` limpios, mismo baseline de 21 issues preexistentes.
- **`/login`, `/register`, `/signup-gym` rediseñadas — hero de dos columnas, 2026-07-22** — pedido explícito con capturas de referencia (carrusel de ejemplos de diseño de producto SaaS, un landing con hero+mockup): "adaptala a tipo gym como socio y cliente". Sesión de scoping corta (`AskUserQuestion`) resolvió dos ambigüedades: dónde aplicarlo (las 3 páginas de auth, no un dashboard) y qué mostrar en el mockup decorativo (vista de reservas/clases, no un dashboard de métricas). Deliberadamente **no** se recreó la ilustración 3D específica de la referencia (evitar reproducir un asset visual distintivo de terceros) — se construyó una composición original a partir de los propios tokens/datos de la app.
  - `AuthHeroLayout` (`src/components/auth/AuthHeroLayout.tsx`, nuevo) — wrapper compartido por las 3 páginas: grid de 2 columnas en `lg:` (título/eyebrow/descripción + el formulario real de cada página, dentro de la misma tarjeta `bg-ink-850` que ya existía) y `ClassScheduleMockup` a la derecha, oculto debajo de `lg`. Cada página le pasa su propio copy (`eyebrow`/`title`/`description`) pero **ninguna** cambió su estado, sus handlers ni sus llamadas a `apiFetch` — es puramente un cambio de contenedor visual.
  - `ClassScheduleMockup` (`src/components/auth/ClassScheduleMockup.tsx`, nuevo) — tarjeta estática sin fetch (datos de ejemplo hardcodeados) que imita "Tus clases" con barras de ocupación por clase, más dos chips flotantes ("Ocupación 94%", "Socios activos +142"). Construido 100% con los tokens de tema existentes (`bg-ink-850`, `border-ink-line`, `text-cream-*`, `bg-wood-*`, `bg-neon-400`, `text-success`, `bg-pop`), así que hereda automáticamente ambos temas sin lógica adicional. **Los chips flotantes usan offsets negativos grandes a propósito** (`-top-8`/`-left-10`, `-bottom-10`/`-right-6`) para quedar completamente fuera del borde de la tarjeta — un primer intento con offsets chicos (`top-10`/`-left-6`) los dejaba flotando *encima* del contenido de la tarjeta (tapaban el título "Tus clases" y la fila "Pilates"), visible solo al verificar con captura de pantalla real, no evidente leyendo el JSX. Si se ajustan estos chips, verificar con captura, no solo con el código.
  - Verificado en vivo en las 3 páginas, tema oscuro y claro, y en viewport mobile (375px, mockup oculto y formulario a ancho completo sin roturas). `npx tsc --noEmit`, `npm run build` y `npm run lint` limpios (mismo baseline de 21 issues preexistentes, ninguno en los archivos tocados).
- **Modo oscuro: de gris plateado a morado/magenta, 2026-07-22 (mismo día, feedback siguiente sobre el hero de arriba)** — el usuario probó el hero nuevo y pidió, con dos referencias visuales reales adjuntas (un dashboard "Lumaora" morado profundo casi negro con acento violeta/magenta, y el propio dashboard de esta app en su plateado anterior), reemplazar el modo oscuro **en toda la app**, no solo en las páginas de auth (confirmado con `AskUserQuestion`: "Toda la app" vs. "Solo las 3 de auth"), más alinear/mejorar el hero de `/login` y `/signup-gym`, que se veía "desigual" y "no muy llamativo".
  - **Reskin resuelto 100% a nivel de tokens** (`src/app/globals.css`, bloque `:root`, tema oscuro) — mismo mecanismo que el fix de "plateado" del 2026-07-21, así que se propagó solo con este cambio a dashboard/sidebar/portal/auth sin tocar componente por componente (ya todo el código usa `bg-ink-*`/`text-cream-*`/`bg-wood-*`/`text-neon-*`, nunca colores sueltos, confirmado al revisar `sidebar.tsx`/`dashboard/page.tsx` antes de tocar nada). Rampa `--t-ink-950/900/850/800/700` con tinte violeta en vez de neutro (`#0d0a17`→`#33254f`, fondo casi negro con undertone morado como la referencia). `--t-wood-*` (acento estructural) pasó de azul-índigo a violeta vívido (`#6d28d9`→`#a78bfa`, escala violet de Tailwind). `--t-neon-*` (antes cian, el "segundo acento") pasó a magenta/fucsia (`#d946ef`→`#f0abfc`) para el combo morado+rosa de la referencia — esto también cambia el color de todos los links de la app (usan `text-neon-400`), a propósito, es el mismo token. `--t-cream`/`-muted`/`-faint` (texto) se corrieron unos grados hacia blanco-lavanda (`#f5f3ff`) en vez de cream cálido, para el "blanco" que pidió el usuario ("es como morado y blanco"). `--t-sidebar-top`/`-deep` y `.sidebar-scope` (los overrides que mantienen el sidebar siempre oscuro sin importar el tema elegido) se actualizaron a la misma familia. **El tema claro no se tocó** — el pedido fue específicamente sobre el modo oscuro.
  - **Botón primario y nav activo del sidebar ganan degradado** (`Button.tsx` variante `primary`, `sidebar.tsx` item activo y "Mi Perfil") — de un fondo sólido `bg-wood-600` a `bg-gradient-to-r from-wood-600 to-neon-500` (violeta→magenta) con `shadow-wood-600/30` — esto fue lo que resolvió el "no está muy llamativo": un cambio de paleta sin esto se veía correcto pero plano, el degradado en los dos elementos más interactivos de la UI (CTA principal, ítem de nav activo) es lo que da el "pop" de la referencia. El nav activo también pasó de un indicador de borde izquierdo (`border-l-2`) a un pill redondeado (`rounded-xl`) con el fondo en degradado, mismo lenguaje que el "Home" resaltado de la referencia.
  - **`AuthHeroLayout` — alineación e impacto visual** (mismo archivo del punto anterior, no uno nuevo): `items-center` → `items-start` en el grid de 2 columnas — con `items-center`, un formulario más corto que el mockup (login) centraba bien, pero uno más largo (signup-gym) dejaba el mockup vertical mente descolgado respecto al inicio del formulario, que es probablemente lo que el usuario leyó como "desigual"/mal alineado entre páginas. El mockup ahora además es `lg:sticky lg:top-16` — en signup-gym (el formulario más largo, con scroll) acompaña el scroll en vez de quedar huérfano arriba apenas se hace scroll. Se sumaron dos blobs difuminados de fondo (`bg-wood-600/25`/`bg-neon-500/20`, `blur-[100px]`, position absolute, todo el layout) y el título ahora es texto con degradado (`bg-gradient-to-r from-cream to-wood-400 bg-clip-text text-transparent`) en vez de un color sólido — mismo criterio que el punto anterior, la paleta sola no alcanzaba para el efecto "llamativo" pedido.
  - Verificado en vivo: `/login` y `/signup-gym` en modo oscuro (paleta morado/magenta con degradados, blobs de fondo, mockup sticky acompañando el scroll del formulario largo) y modo claro (sin cambios, confirmado que `--t-wood-*`/`--t-neon-*` del tema claro no se tocaron); `/dashboard` con una sesión `SUPER_ADMIN` simulada (`localStorage`, sin login real, ya que no hay credenciales de prueba disponibles) — sidebar con el pill degradado en el ítem activo, fondo/tarjetas en la nueva rampa morada, badge de rol y links en magenta, tarjetas "Próximamente" con sus bordes punteados de colores semánticos originales sin cambios (no forman parte del sistema de tokens, no era parte del pedido). Sesión de prueba limpiada de `localStorage`/cookies después de verificar. `npx tsc --noEmit`, `npm run build` y `npm run lint` limpios, mismo baseline de 21 issues preexistentes, ninguno en los archivos tocados.
- **`MercadoPagoConnectionCard`: de botón OAuth a formulario de token pegado a mano, 2026-07-23** — el backend abandonó el flujo OAuth de conexión (Mercado Pago exige empresa en Argentina para crear la Aplicación que ese flujo necesitaba, no viable para Ecuador — ver `CLAUDE.md` del backend para el detalle completo). El componente ya no hace `window.location.href = data.authorizationUrl` a un checkout hosted — ahora es un `<form>` con dos campos `Input type="password"` (Access Token y Secreto del webhook, ambos sensibles) que llama `PUT /gyms/:id/mercadopago/credentials`, mismo patrón de `error`/`isLoading` que ya tenía el componente y que usa `CreateGymModal` en otros lados de la app. Se agregó copy instructivo con los dos pasos manuales que el dueño del gimnasio tiene que hacer en su propia cuenta de Mercado Pago antes de pegar los valores acá (sacar el Access Token, configurar el webhook con la URL armada desde `NEXT_PUBLIC_API_URL` y copiar el secreto de firma) — a diferencia del flujo OAuth viejo, ahora hay pasos manuales reales del lado del gimnasio, no alcanza con un botón.
  - Se eliminó `MercadoPagoStatusBanner` de `/dashboard/settings` (leía `?mercadopago=connected|error` de la URL) — ya no hay redirect de vuelta, el resultado se muestra síncronamente dentro de la propia tarjeta apenas responde el `PUT`.
  - Sin cambios en `GymDetailPage`/`GymTable`/`PlansSection` — todos derivan de `gym.mercadoPagoUserId`, que sigue existiendo con el mismo significado (ahora poblado desde `GET /users/me` en vez de la respuesta del code exchange de OAuth).
  - Verificado en vivo end-to-end contra el backend y la base de datos reales (fixture temporal vía `POST /auth/signup-gym`, borrado después): el formulario nuevo, con un token inválido a propósito, muestra el error real que devuelve el backend ("El access token no es válido o fue revocado...") — confirma que el camino completo (formulario → `PUT` → validación real contra la API de Mercado Pago → error mostrado) funciona. `npx tsc --noEmit`, `npm run build` y `npm run lint` limpios, mismo baseline de 21 issues preexistentes, ninguno en los archivos tocados.
- **Fase 1 del roadmap post-v1.5: Check-in, Dashboard financiero, Lista de espera — UI, 2026-07-24** — consume los 3 endpoints nuevos del backend (ver `CLAUDE.md` de `smartbox-backend` para el detalle de `POST /checkins`, `GET /reports/finance`, `POST/DELETE /waitlist`), sin contrato nuevo del lado frontend más allá de leer las respuestas. Sucursales (el 4° ítem de Fase 1) queda fuera, deliberadamente pospuesto a su propia sesión de scoping.
  - `ReservationTable` (compartida por `/dashboard/reservations` y `/portal/reservations`) gana props opcionales `showCheckIn`/`activeCheckIns`/`onCheckInChange` — botón "Check-in" en cada reserva `confirmed` cuya fecha es hoy (`isToday()`, comparación de fecha local, no UTC), que llama `POST /checkins {reservationId}` y cambia a "Check-out" (llama `POST /checkins/:id/checkout`) una vez usado; `activeCheckIns` es un `Map<reservationId, checkInId>` que `ReservationsContent` arma con un solo `GET /checkins` al montar, no un fetch por fila.
  - `FinanceReportCard` (`src/components/reports/`, nuevo) — mismo patrón visual que `RevenueReportCard` (números grandes, sin librería de gráficos, ninguna instalada en el proyecto), grid 2×2 con MRR/ARR/Churn/LTV. `ltvCents === null` se muestra como "—" con la leyenda "Sin datos suficientes" en vez de intentar formatear `Infinity`. Se agrega a `ReportsSection` junto a ocupación/ingresos, gateado por el mismo prop `canViewRevenue` ya existente (mismo nivel de sensibilidad que ingresos — `GET /reports/finance` es `ADMIN`-only en el backend).
  - `MyWaitlistSection` (`src/components/reservations/`, nuevo, CLIENT-only) — lista `GET /waitlist/me` con botón "Salir de la lista" (`DELETE /waitlist/:id`), renderizada por `ReservationsContent` solo cuando el usuario es `CLIENT`. `ClassDetailContent` gana un botón "Anotarme en lista de espera" que aparece cuando `slot.available === 0` (mismo dato que ya usaba para deshabilitar "Reservar"), llama `POST /waitlist {classId, startAt}`.
  - Verificado con `npx tsc --noEmit`/`npm run build`/`npm run lint` limpios (mismo baseline de 21 issues preexistentes, ninguno en los archivos tocados) y en vivo en navegador contra el backend/base de datos reales (fixture temporal: gimnasio, admin, socio, clase capacidad 1, reserva confirmada para hoy): login como el socio, botón "Check-in" visible en `/portal/reservations` sobre la reserva de hoy, clic lo cambia a "Check-out" manteniendo el estado "Confirmada". El botón de lista de espera y la promoción FIFO se probaron de punta a punta vía un script E2E contra la API real (segundo socio anotado en lista de espera de una clase llena, promovido automáticamente a `Reservation` `confirmed` al cancelar el primero, desaparece de `GET /waitlist/me`) — no se armó un segundo fixture de navegador solo para ver el botón, ya estaba probado el flujo completo por API. Fixture del navegador eliminado de la base de datos después de verificar.

## ⚠️ Este Next.js no es el que conocés

Next.js 16 renombró `middleware.ts` → **`proxy.ts`** (función exportada `proxy`, no `middleware`). `middleware.ts` sigue funcionando pero está deprecado y lo marca así el propio dev server. Este repo ya usa la convención correcta (`src/proxy.ts`) — no lo "corrijas" de vuelta a `middleware.ts`, ya pasó una vez. Ante cualquier otra duda de convención de Next.js, revisá `node_modules/next/dist/docs/` antes de asumir por entrenamiento previo (ver `AGENTS.md`).

## Stack y comandos

```bash
npm run dev      # next dev -p 3000
npm run build
npm run lint      # eslint (config-next core-web-vitals + typescript)
npx tsc --noEmit  # typecheck
```

Sin infraestructura de tests todavía (ni Jest ni Vitest instalados) — ver Epic 0 / E0-12 en el documento oficial.

## Convenciones establecidas — no las rompas sin razón

- Todas las llamadas al backend pasan por `apiFetch` (`src/lib/api.ts`), que agrega el `Authorization: Bearer` desde `localStorage` y maneja el 401 → logout automático.
- Estado de sesión: `AuthContext` (`src/context/AuthContext.tsx`). Usá `updateUser(partial)` para reflejar cambios del propio usuario (nombre, etc.) sin forzar un re-login — no dupliques la lógica de `localStorage.setItem('user', ...)` en cada componente.
- El JWT vive en `localStorage` + una cookie no-httpOnly (para que `proxy.ts` pueda leer el rol) — es un gap de seguridad conocido y documentado (ver Recomendaciones / Epic 0), no lo "arregles" a medias sin revisar el resto del flujo de auth.
- Guards de ruta por rol: `src/proxy.ts` lee las cookies `token`/`userRole` — si agregás una ruta nueva bajo `/dashboard`, sumala a `rolePermissions` ahí, no confíes solo en que el sidebar la oculte (eso es UX, no seguridad).
- Nada de `console.log` de depuración, especialmente si loguea tokens o payloads de auth (`console.error` para manejo real de errores sí está establecido, ver `UserTable.tsx`/`GymsPage`).
- Antes de asumir que un endpoint del backend existe, confirmalo contra el API Contract del documento oficial o contra Swagger (`/docs` del backend) — el backend (Epics 0-5) va muy adelante de este frontend, así que lo más probable es que el endpoint SÍ exista y solo falte la página acá.
- Patrón para un módulo nuevo que consume el backend (ver `src/app/dashboard/gyms/`, `src/components/gyms/` como referencia): tipo en `src/types/index.ts`, página(s) bajo `src/app/dashboard/<módulo>/`, componentes de UI en `src/components/<módulo>/`, todo vía `apiFetch`; agregar el permiso de ruta en `src/proxy.ts` y la entrada en el sidebar (`src/components/ui/sidebar.tsx`) en el mismo cambio — no alcanza con uno solo de los dos.
- Cualquier página que use `useSearchParams()` de `next/navigation` necesita un `<Suspense>` alrededor (`npm run build` falla si no) — extraé la parte que lo usa a un componente aparte si el resto de la página no lo necesita (ver `dashboard/settings/page.tsx`).
- Deuda de lint preexistente, no introducida por sesiones recientes — no la arregles de paso dentro de un cambio no relacionado, es su propia historia: `role: any` en el `User` de `src/types/index.ts`, algunos `any`/imports sin usar sueltos, y `react-hooks/set-state-in-effect` (React Compiler) en casi cualquier página que siga el patrón `useEffect(() => { load(); }, [load])` — es el patrón de carga de datos establecido en todo el dashboard (`users/page.tsx`, `AuthContext.tsx`, `gyms/`, `settings/`, `membership/`, etc.), así que no vale la pena pelearlo página por página; medí contra `git stash` antes de asumir que un cambio tuyo sumó deuda nueva, `npm run lint` ya marca ~17 problemas en el baseline actual sin tocar nada.

## Definition of Done (ver documento oficial §06 para el detalle completo)

- [ ] `npx tsc --noEmit` sin errores
- [ ] `npm run lint` sin errores nuevos
- [ ] Probado en el navegador de punta a punta (login real si el flujo lo requiere), no solo que compile
- [ ] Sin `console.log` ni datos fabricados presentados como reales (si un módulo no está listo, un estado "Próximamente" explícito, no una tarjeta con números inventados)
- [ ] Commits en este repo: **sin** el trailer `Co-Authored-By`
- [ ] Formato de commit [Conventional Commits](https://www.conventionalcommits.org/): `<tipo>(<alcance>): <descripción>` (ej.: `fix(dashboard): remove fabricated metrics`). Tipos: `feat`, `fix`, `docs`, `style`, `refactor`, `perf`, `test`, `build`, `ci`, `chore`, `revert`.
